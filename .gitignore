# Build artifacts (regenerated by build.ps1)
build/
build_verify/
dist/
dist_verify/
*.spec
__pycache__/
*.py[cod]

# PyInstaller build venv
buildenv/
.venv/
venv/

# Third-party tools (download separately)
upx/

# SECRETS - never commit
*.pfx
*.pem
*.key

# Old versions / backups
Old Versions/

# Distribution (the .exe is published via GitHub Releases, not committed)
*.exe

# Local notes / Windows cruft
desktop.ini
compile-guide.txt
compile-oneliner.txt
datas_spec.txt
sign program.txt
build.*
gsi_capture.py